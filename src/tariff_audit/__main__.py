"""Entry point for `python -m tariff_audit` and the `tariff-audit` CLI script."""

from __future__ import annotations


def main() -> None:
    print("TariffAudit FL — Phase 1 foundation. CLI not yet implemented.")
    print("Run `pytest` to verify tariff calculations, or import from `tariff_audit` directly.")


if __name__ == "__main__":
    main()
